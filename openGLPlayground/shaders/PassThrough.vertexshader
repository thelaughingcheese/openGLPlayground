#version 330 core

// Input vertex data, different for all executions of this shader.
layout(location = 0) in vec3 PosIn;

out vec2 UV;

void main(){
    gl_Position = vec4(PosIn,1);
	UV = (PosIn.xy+vec2(1,1))/2.0;
}

